spring:
  cache:
    type: caffeine
    caffeine:
      spec: maximumSize=1000,expireAfterWrite=5m
    cache-names:
      - searchTerms

  # Redis configuration (if using distributed cache)
  data:
    redis:
      host: localhost
      port: 6379
      timeout: 2000ms
